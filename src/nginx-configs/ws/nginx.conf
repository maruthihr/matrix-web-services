events {
    worker_connections 1024;
}

http {

    server {
        listen 53213;
        server_name ws;
    
        location / {
            proxy_pass http://ws;
            proxy_set_header Host $host;
        }
    }


    upstream ws {
        server 172.17.0.24:3000;
    }
}
